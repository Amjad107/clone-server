import 'package:flutter/material.dart';

class ScanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Scan Problem')),
      body: Center(child: Text('Image scanning feature coming soon...')),
    );
  }
}
